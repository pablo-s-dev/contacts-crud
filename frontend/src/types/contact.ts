export type Contact = {
  id: string;
  name: string;
  email: string;
  phone: string;
  createdAt: string;
  updatedAt: string;
};

export type ContactInput = {
  name: string;
  email: string;
  phone: string;
};
